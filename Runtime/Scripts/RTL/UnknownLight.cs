using UnityEngine;

namespace NuGizWrap.Lighting
{
    public class UnknownLight : MonoBehaviour
    {
        public int unk1, unk2, unk3, unk4, unk5, unk6, unk7;
        public Vector3 unk8;
        public int unk9, unk10, unk11, unk12;
        public byte[] unk13;

        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}